namespace KemiaSimulatorEnvironment.Object {
    /*public enum InvokeEventType {
        /// <summary>
        /// fullfill invoke with an order added to player's orders container
        /// </summary>
        ADD_CUSTOMER_ORDER,
        /// <summary>
        /// fullfill invoke with an object used and being removed from player inventory if possible
        /// </summary>
        USE_ITEM,
    }*/
}